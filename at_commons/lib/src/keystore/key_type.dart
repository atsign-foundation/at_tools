enum KeyType {
  selfKey,
  sharedKey,
  publicKey,
  privateKey,
  cachedPublicKey,
  cachedSharedKey,
  invalidKey
}

enum ReservedKey {
  encryptionSharedKey,
  encryptionPublicKey,
  encryptionPrivateKey,
  pkamPublicKey,
  signingPrivateKey,
  signingPublicKey,
  nonReservedKey
}
