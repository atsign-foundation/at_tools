enum KeyType {
  selfKey,
  sharedKey,
  publicKey,
  privateKey,
  cachedPublicKey,
  cachedSharedKey,
  reservedKey,
  invalidKey
}

enum ReservedKeys {
  encryptionSharedKey,
  encryptionPublicKey,
  encryptionPrivateKey,
  pkamPublicKey,
  signingPrivateKey,
  signingPublicKey,
  nonReservedKey
}
